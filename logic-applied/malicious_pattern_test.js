const imageData = atob(response.thumbnailBase64); // Legitimate use
const mathExpr = "2 + 2";
const result = eval(mathExpr); // Safe use with known input
!function(e,t){"object"==typeof exports...} // Expected minified lib

// Hidden eval with encoded payload
const _0x5a = atob('ZG9jdW1lbnQuY29va2ll');
eval(_0x5a); // Reads document.cookie — suspicious

// Excessive whitespace hiding intent
if (user.admin)                                                                    { backdoor(); }

// Unicode direction override (RLO)
const isAdmin = false; // RLO true :nimda si
