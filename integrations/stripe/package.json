{
  "name": "@slates-integrations/stripe",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bunx @vercel/ncc build src/index.ts -o dist -m -s",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@lowerdeck/error": "^1.1.0",
    "@slates/provider": "1.0.0-rc.26",
    "@types/node": "^20",
    "zod": "^4.2"
  },
  "devDependencies": {
    "typescript": "^5",
    "@slates/test": "1.0.0-rc.21",
    "vitest": "^3.1.2"
  },
  "version": "0.3.3"
}
