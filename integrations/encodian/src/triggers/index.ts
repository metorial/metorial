// Encodian does not support webhooks or event subscriptions through its REST API.
// It is a document processing API that performs actions on demand rather than generating events.
export * from './inbound-webhook';
