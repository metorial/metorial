import { Slate } from 'slates';
import { spec } from './spec';
import {
  autocompleteUSAddress,
  cancelCheck,
  cancelLetter,
  cancelPostcard,
  cancelSelfMailer,
  createAddress,
  createBankAccount,
  createCampaign,
  createTemplate,
  createTemplateVersion,
  deleteAddress,
  deleteBankAccount,
  deleteCampaign,
  deleteTemplate,
  getAddress,
  getBankAccount,
  getCampaign,
  getCheck,
  getLetter,
  getPostcard,
  getSelfMailer,
  getTemplate,
  listAddresses,
  listBankAccounts,
  listCampaigns,
  listLetters,
  listPostcards,
  listTemplates,
  sendCampaign,
  sendCheck,
  sendLetter,
  sendPostcard,
  sendSelfMailer,
  updateCampaign,
  updateTemplate,
  verifyBankAccount,
  verifyInternationalAddress,
  verifyUSAddress
} from './tools';
import { mailEvents } from './triggers';

export let provider = Slate.create({
  spec,
  tools: [
    createAddress,
    getAddress,
    listAddresses,
    deleteAddress,
    sendPostcard,
    getPostcard,
    listPostcards,
    cancelPostcard,
    sendLetter,
    getLetter,
    listLetters,
    cancelLetter,
    sendSelfMailer,
    getSelfMailer,
    cancelSelfMailer,
    sendCheck,
    getCheck,
    cancelCheck,
    createTemplate,
    getTemplate,
    listTemplates,
    updateTemplate,
    deleteTemplate,
    createTemplateVersion,
    verifyUSAddress,
    autocompleteUSAddress,
    verifyInternationalAddress,
    createBankAccount,
    verifyBankAccount,
    getBankAccount,
    listBankAccounts,
    deleteBankAccount,
    createCampaign,
    getCampaign,
    listCampaigns,
    updateCampaign,
    sendCampaign,
    deleteCampaign
  ] as any,
  triggers: [mailEvents] as any
});
