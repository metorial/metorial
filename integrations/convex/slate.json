{
  "name": "@metorial/convex",
  "description": "Invoke serverless queries, mutations, and actions on a reactive document-relational database. Read, insert, update, and delete documents across tables with ACID transactions. Upload and manage files in built-in storage. Schedule functions for future or recurring execution (cron jobs). Perform full-text search with BM25 relevance scoring and vector similarity search for AI/RAG use cases. Stream export document-level changes (inserts, updates, deletes) for incremental data sync to external systems. Stream import data from external sources into Convex tables. Manage projects, deployments, deploy keys, and environment variables via the Management API. Define custom HTTP endpoints for inbound webhooks and REST-style APIs.",
  "categories": ["apis-and-http-requests"],
  "skills": [
    "invoke serverless functions",
    "query and mutate documents",
    "upload and manage files",
    "schedule future tasks",
    "full-text search documents",
    "vector similarity search",
    "stream export data changes",
    "stream import external data",
    "manage projects and deployments",
    "create custom HTTP endpoints"
  ]
}
