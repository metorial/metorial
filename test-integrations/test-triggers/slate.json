{
  "name": "@__test__/triggers",
  "description": "Internal test slate for polling, callbacks, and the comprehensive core, preset, and provider-boundary webhook verification matrix.",
  "categories": ["utilities"],
  "skills": [
    "receive webhook payloads and emit processed trigger events",
    "emit the current time during polling runs",
    "test asynchronous and synchronous callback trigger delivery",
    "verify static-token, raw HMAC, and Ed25519 callback authentication",
    "verify canonical webhook presets, replay controls, and bootstrap flows",
    "exercise scoped synthetic provider-verification boundaries",
    "run valid, invalid, and dry-run webhook verification observations"
  ]
}
